import classes
from ines import *
from josephine import *
from pynput import keyboard


while True:
    """
    objectif général :
    move(player)
    analyser_case (recup les objets)
    if combat :
        combat
    else :
        move(entities)
    update(map)
    display(map)"""
